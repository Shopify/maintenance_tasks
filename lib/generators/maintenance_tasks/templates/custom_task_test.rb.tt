# frozen_string_literal: true
require 'test_helper'

module <%= tasks_module %>
<% module_namespacing do -%>
  class <%= class_name %>TaskTest < ActiveSupport::TestCase
    # test "#process performs a task iteration" do
    #   <%= tasks_module %>::<%= class_name %>Task.process(element)
    # end

    # test "#enumerator_builder knows how to enumerate the custom collection" do
    #   <%= tasks_module %>::<%= class_name %>Task.enumerator_builder
    # end
  end
<% end -%>
end
